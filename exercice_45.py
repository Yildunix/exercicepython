jour = float(input("Quelle est le jour que vous demandez ? : "))
mois = input("Quelle est le mois ? : ")

if mois == 'janvier' :
    if jour == 1 : 
        print ("Jour de l'an")
if mois == 'juillet' :
    if jour == 1 :
        print("fete du canada")
if mois == 'decembre' :
    if jour == 25: 
        print("Noel")
